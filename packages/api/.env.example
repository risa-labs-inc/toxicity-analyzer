# Server Configuration
PORT=8080
NODE_ENV=development

# Database Configuration (Local Development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=toxicity_analyzer_dev
DB_USER=postgres
DB_PASSWORD=postgres

# Database Configuration (Production - Cloud SQL)
# Uncomment for production deployment
# DB_HOST=/cloudsql/toxicity-analyzer:us-central1:toxicity-analyzer-db
# DB_NAME=toxicity_analyzer
# DB_USER=toxicity_user
# DB_PASSWORD=<from Secret Manager>

# Firebase Configuration
FIREBASE_PROJECT_ID=toxicity-analyzer

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGIN=*

# Logging
LOG_LEVEL=info
